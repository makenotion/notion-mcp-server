version: '3.8'

services:
  notion-mcp-server:
    build: .
    environment:
      OPENAPI_MCP_HEADERS: '{"Authorization": "Bearer ntn_XXXXX", "Notion-Version": "2022-06-28"}'
    stdin_open: true
    tty: true
    restart: unless-stopped 